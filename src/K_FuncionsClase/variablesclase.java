package K_FuncionsClase;

public class variablesclase {
/*
    // EL US DE LES VARIABLES GLOBALS ELS HI HA QUE RESTRINGIR EN LO MAXIM DEL POSSIBLE. 
        // Si tinc 100 funcions que necessiten la mateixa variable, si que es recomanable per no sobrecargar el pase de parametros. 
        // Hem de pensar si es imprescindible o prescindible. 

    // Esta variable es global, significa que es veu desde tota la classe. 
    // Si poso public, puc accedir desde fora de la classe, desde totes les funcions de totes les clases
    // del paquet. Si ho hem de fer servir aqui dins, no ens ha de preocupar. Estatic significa que no he deccrear
    // un objecte de la clase per a que esta variable no existeixi. 
    private static int x = 10; 
    static boolean primeroMayor(int a, int b){
        // Si la senyalem veurem que el VSCode la referencia a la estatica. Desde fora de la classe tambe es te accés. 
        // Private només desde aquí. 
        // Les variables locals oculten cualsevol variable global que tingue el mateix nom. Si poso "x" tindra
        // prioritat esta en mes de la global. 

        x = 12;
        return a > b;
    }
    public static void main (String[] args){
        int y = 12;  // Variable Local propia de la funció. Una funcio es com una caixa negra. 
        int x = 17;  // Al ser esta local, no sera ocultada. Local i local no en ambits diferents. 
        System.out.println("La variable x de main = " + x);
        System.out.println("La variable x de main = " + y);
        for (int n=1; n<=10;n++){
            System.out.println(n+", ");
        while (y>4){
            // Una variable fora del seu ambit, pot ocultar una global igualment. 
            int x = 12; // No es pot usar fora del bucle. L'ambit d'una variable es queda dins les claus. 
            System.out.println(x+", ");
        }
        }
        System.out.println("\nLa variable n de main() = " + j);
        // Aqui ens donara error perque no reconeix la variable, perque esta dintre una altra funcio i no fora. 
        // System.out.println("La variable y de main = " + y);
        */
    }


